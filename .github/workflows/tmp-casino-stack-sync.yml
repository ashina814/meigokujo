name: Temporary Casino Stack Sync

on:
  push:
    branches: ["feat/casino-chinchiro-prehold"]
    paths: [".github/workflows/tmp-casino-stack-sync.yml"]

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/casino-chinchiro-prehold
          fetch-depth: 0
      - name: Merge latest PR10 into PR11 branch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin feat/casino-auto-chip-flow
          git merge --no-ff origin/feat/casino-auto-chip-flow -m "chore: sync PR10 into PR11"
          git rm .github/workflows/tmp-casino-stack-sync.yml
          git commit --amend --no-edit
          git push origin HEAD:feat/casino-chinchiro-prehold
